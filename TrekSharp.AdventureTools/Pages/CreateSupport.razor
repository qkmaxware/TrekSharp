@page "/new/support/custom"
@inherits Page

<LCARS Title="CUSTOM  SUPPORT CHARACTER">
    <SupportWizard OnSupportCreated=save></SupportWizard>
</LCARS>

@code {
    private void save(SupportCharacter c) {
        Data.Supports.Add(c);
        this.NaviateToSupportCharacter(c);
    }
}